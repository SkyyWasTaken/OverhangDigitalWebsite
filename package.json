{
  "name": "@overhang/index",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "get-beta-creds": "aws sts get-caller-identity --profile FullAdmin-805673386085",
    "get-prod-creds": "aws sts get-caller-identity --profile FullAdmin-903070443001",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir build && npm run build --ws",
    "lint": "npm run lint --ws",
    "dev": "npm run dev -w packages/website",
    "cdk": "npm run cdk -w packages/cdk",
    "start": "cd ./packages/website && vite",
    "clean": "rm -rf ./node_modules && rm -rf ./cdk.out && rm -rf ./website"
  },
  "workspaces": [
    "./packages/website", "./packages/api", "./packages/cdk"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "web-vitals": "^5.1.0"
  }
}
